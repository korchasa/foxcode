{
  "mcpServers": {
    "foxcode": {
      "command": "sh",
      "args": ["-c", "export FOXCODE_PROJECT_DIR=\"$PWD\" && cd foxcode/channel && npm ci --omit=dev --silent 2>/dev/null && node server.mjs"]
    }
  }
}
